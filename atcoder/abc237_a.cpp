#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <bits/stdc++.h>
#include <limits.h>

using namespace std;

int main() {
    long long N;
    cin >> N;
    if (N <= INT_MAX && N >= INT_MIN) {
        cout << "Yes\n";
    }
    else
        cout << "No\n";
}
